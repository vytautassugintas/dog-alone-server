type Query {
  hello: String
  host: Host
}

"Describes server host"
type Host {
  "Server machine name"
  hostname: String
  "Server machine IPv4 address"
  ipv4: String
  "Server machine IPv6 address"
  ipv6: String
}
